import 'package:flutter/material.dart';

class TaskFoldersScreen extends StatelessWidget {
  static String id = 'folders';
  const TaskFoldersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Container(
        child: Text('YOOO!'),
      ),
    );
  }
}
